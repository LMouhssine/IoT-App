name: iot_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # ===== Core Flutter =====
  cupertino_icons: ^1.0.8

  # ===== Firebase Suite =====
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  cloud_firestore: ^4.17.5
  firebase_analytics: ^10.10.7
  firebase_messaging: ^14.9.4
  firebase_storage: ^11.7.7

  # ===== Data Visualization =====
  fl_chart: ^0.66.2           # Remplacement de charts_flutter

  # ===== State Management =====
  provider: ^6.1.2

  # ===== Date Formatting =====
  intl: ^0.18.1

  # ===== Networking & Storage =====
  http: ^1.2.1
  shared_preferences: ^2.2.3

  # ===== Device Features =====
  connectivity_plus: ^6.0.3
  device_info_plus: ^10.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
